import { Card, CardContent, CardTitle } from "@/components/ui/card.tsx";

const TotalPayment = () => {
  return (
    <Card className={"px-4"}>
      <CardTitle variant={"md"}>Total Money Owed</CardTitle>
      <CardContent></CardContent>
    </Card>
  );
};

export default TotalPayment;
